export enum ROUTE_PATH {
   // ** public
   LOGIN = "/login",
   SIGNUP = "/signup",
   NOT_FOUND = "*",

   // ** private
   HOME = "/home",
   EMAIL = "/email",
   PROJECTS = "/projects",
   STATISTICS = "/statistics",

   CALENDAR = "/calendar",
   TASKS = "/tasks",
   NOTES = "/notes",
   TABLES = "/tables",
   DOCUMENTS = "/documents",
   
   TRASH = "/trash",
   SETTINGS = "/settings",

   TEAMS = "/teams",
   MESSENGER = "/messenger",
}